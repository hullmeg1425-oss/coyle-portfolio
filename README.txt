# Margaret Coyle Digital Portfolio

Open `index.html` in a browser to preview the portfolio.

The site is a static HTML/CSS portfolio with the supplied FamilyLife screenshots in `assets/`.
It can be published on any static web host. Upload the entire folder, keeping the `assets` folder beside `index.html`.
